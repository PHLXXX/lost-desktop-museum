# 社区离线行为

社区同步需要网络，索引和已读取详情缓存在 IndexedDB。六小时内不会重复自动同步，用户可手动刷新；网络、404、JSON 或 Schema 错误时优先显示上次有效缓存，并明确标记“正在使用上次同步的社区目录”。没有缓存时只显示可恢复空状态。

社区失败不影响内置案件、本地导入案件、已安装社区案件、玩家存档或档案工坊。离线时下载和更新禁用，已安装案件可正常开始和保存。新鲜安装且没有缓存时，社区目录无法浏览。本阶段没有 Service Worker 或完整 PWA，因此浏览器必须仍能加载主应用页面本身；已加载应用或浏览器缓存中的应用可以使用上述目录缓存。
