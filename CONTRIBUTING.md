# 贡献指南

1. 从 `main` 创建短生命周期分支。
2. UI 使用中文，代码标识符与文件名使用英文。
3. 案件内容只放在 `src/cases`；复杂判断只放在 `src/engine`。
4. 新行为先添加失败测试，再写最小实现。
5. 提交前运行 `npm run check` 与相关 `npm run e2e`。
6. 不提交密钥、隐私数据、外链正式资源、`node_modules`、`dist` 或测试报告。
7. 提交保持小而清晰，Pull Request 说明玩家可见变化和验证结果。

